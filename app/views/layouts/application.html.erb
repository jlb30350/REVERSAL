<!DOCTYPE html>
<html>
<head>
  <title>AppReservationssalles</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>  <!-- Balises pour la sécurité CSRF -->
  <%= csp_meta_tag %>     <!-- Politique de sécurité du contenu -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <%= stylesheet_link_tag "styles", "data-turbo-track": "reload" %>  <!-- Lien vers la feuille de styles CSS -->
  <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload' %> <!-- Lien vers les fichiers JavaScript -->
</head>
<body>
  <div class="header">
    <% if current_user %>
      <!-- Affiche ce bouton si l'utilisateur est connecté -->
      <div class="auth-buttons">
        <%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, class: "btn" %>
      </div>
    <% else %>
      <!-- Affiche ce bouton si aucun utilisateur n'est connecté -->
      <div class="auth-buttons">
        <%= link_to 'Connexion', new_user_session_path, class: "btn" %>
      </div>
    <% end %>
    <div class="logo-container">
      <!-- Emplacement pour votre logo -->
    </div>
  </div>

  <div class="body-content">
    <%= yield %>  <!-- Cet endroit affichera le contenu des autres vues -->
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
